rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Deny all access by default. Grant granular permissions per collection.
      allow read, write: if false;
    }
  }
}
