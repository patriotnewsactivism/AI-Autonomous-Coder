{
  "name": "ai-autonomous-agent-functions",
  "description": "Cloud Functions for Firebase for the AI Autonomous Agent System project.",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "echo 'No lint configuration for functions yet'",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "build": "echo 'No build step configured for functions'"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.9.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.0.0"
  },
  "private": true
}
